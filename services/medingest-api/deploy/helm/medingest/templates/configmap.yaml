apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "medingest.fullname" . }}-config
data:
  MINIO_ENDPOINT: {{ .Values.config.minioEndpoint | quote }}
  NATS_URL: {{ .Values.config.natsUrl | quote }}
  TEMPORAL_URL: {{ .Values.config.temporalUrl | quote }}
